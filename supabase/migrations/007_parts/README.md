# 题库种子（拆分版）

Supabase SQL Editor 无法一次跑完整的 `007_questions_seed.sql`（会报 `Query is too large`）。

## 怎么跑

1. 已执行 `006_practice_schema.sql`、`006b_math_chapters.sql`
2. **先跑** `007a_clean_only.sql`（可练习题，约 74 道）
3. 若还要导入待校对题：按 `007b_review_part_01.sql` → `02` → … 依次新建查询、粘贴、Run

每次只粘贴**一个**文件内容。
